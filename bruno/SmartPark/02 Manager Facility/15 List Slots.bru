meta {
  name: List Slots
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/manager/slots?parkingId={{parkingId}}&page=0&size=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{managerToken}}
}
