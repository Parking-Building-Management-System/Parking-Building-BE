meta {
  name: Delete Pricing Rule
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/manager/pricing/rules/{{pricingRuleId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{managerToken}}
}
