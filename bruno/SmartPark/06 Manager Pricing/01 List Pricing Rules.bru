meta {
  name: List Pricing Rules
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/manager/pricing/rules?parkingId={{parkingId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{managerToken}}
}
