meta {
  name: Update Staff Status
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/manager/staff/{{staffId}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{managerToken}}
}
headers {
  Content-Type: application/json
}

body:json {
  {
    "status": "ACTIVE"
  }
}
