meta {
  name: Update Kiosk Status
  type: http
  seq: 12
}

patch {
  url: {{baseUrl}}/manager/kiosks/{{kioskId}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{managerToken}}
}
headers {
  Content-Type: application/json
}

body:json {
  {
    "status": "ACTIVE"
  }
}
